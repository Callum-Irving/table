import std.io;

interface ToString {
    fun to_string(): str;	

    # Pointer type:
    fun get_ptr(): *self;
}


# This is a comment
fun main() {
    # Some expression statements
    1 + 2;
    a(123, 456, hello + 5);

    # Assignment expression
    a.b.c = -(&f5);

    func();
    
    # Block statement
    {
        io.println(item);
        io.println(item);
    }
    
    # Let definition
    let a: int = 5;
    # Const definition
    const b: str = "hello";

    # user defined type
    const some_var: module.Type = function();
}
